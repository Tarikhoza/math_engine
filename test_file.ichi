// Module syntax:
// module keyword
// name
// list of patterns/arguments
// return type/pattern
// body

formula x_{1/2}={frac{-bpm{sqrt{b^{2}-4ac}}}{2a}}}
indentity BasicIntegration: int{x^{b}} -> frac{x^{b+1}}{b+1} 
indentity ExtractConstant: int{ax^{b}} -> a*int{x^{b}} 

module QuadraticEquation a:x^{2}+bx+c -> (Math, Math):
	b = a => x_{1/2}
	Draw(a, b_0, b_1)
	return result
