use crate::math::geometry::point::Point;
use crate::math::Math;

pub struct Line {
    pub point_a: Point,
    pub point_b: Point,
}

impl Line {
    fn length(&self) -> Math {
        todo!();
    }

    fn distance(&self, other: Point) -> Math {
        todo!();
    }
}
